<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
  <div class="container">
    <%= link_to 'VetClinic', root_path, class: 'navbar-brand' %>
    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <div class="navbar-nav me-auto">
        <%= link_to 'Dueños', owners_path, class: 'nav-link' %>
        <%= link_to 'Mascotas', pets_path, class: 'nav-link' %>
        <%= link_to 'Vets', vets_path, class: 'nav-link' %>
        <%= link_to 'Citas', appointments_path, class: 'nav-link' %>
      </div>

      <div class="navbar-nav">
        <% if user_signed_in? %>
          <% # Mostrar nombre completo del usuario %>
          <span class="nav-link text-light me-2">
            <%= "#{current_user.first_name} #{current_user.last_name}" %>
          </span>
          
          <%= button_to "Sign out", destroy_user_session_path, 
                        method: :delete, 
                        class: "btn btn-outline-light btn-sm" %>
        <% else %>
          <%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
          <%= link_to "Sign in", new_user_session_path, class: "nav-link" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>